/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['datawrapper.dwcdn.net', 'public.flourish.studio'],
  },
}

module.exports = nextConfig
